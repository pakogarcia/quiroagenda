# Build Project Workflow

Use this workflow to install dependencies, run TypeScript type checking, and compile the Next.js production build.

## Steps

1. **Install Dependencies**
   ```bash
   npm install
   ```

2. **Run TypeScript Type Check**
   ```bash
   npm run typecheck
   ```

3. **Build Web Application**
   ```bash
   npm run build
   ```
